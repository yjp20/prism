import * as t from '../../types';

export const filesystemLoader = {
  load: async <Resource>(opts?: t.IFilesystemLoaderOpts): Promise<Resource[]> => {
    // TODO: will use graph
    return [];
  },
};
